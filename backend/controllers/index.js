
const authController = require('./authController');
const sessionController = require('./sessionController');
// Importing modules

// Exporting modules
module.exports = {
    authController,
    sessionController
};